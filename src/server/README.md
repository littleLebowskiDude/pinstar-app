# server

This directory contains tRPC routers and server-side code.

Example files you might create here:
- `routers/` - Directory for tRPC routers
- `trpc.ts` - tRPC initialization and context
- `root.ts` - Root tRPC router that combines all sub-routers
